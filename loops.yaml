- name: loops
  hosts: localhost
  connection: local
  tasks:
  - name: print loops
    ansible.builtin.debug:
      msg: "hello {{ item }}"
    loop:
    - saikiran
    - lakshmi 
    - durga
    - dhaswika
    