- name: variables in files
  hosts: localhost
  connection: local
  vars_files:
    - varconfig.yaml
  tasks:
  - name: pint variables
    #ansible.builtin.debug:
    ansible.builtin.debug:
      msg: " course name: {{ course }}, duration time: {{ duration }}, triner name: {{ trinee }}"
